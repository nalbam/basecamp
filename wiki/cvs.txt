=== 계정 생성 ===
<code>
# adduser cvs
</code>
<code>
$ cd /home/cvs
$ mkdir repo
</code>

=== cvs pserver 생성 ===
<code>
# vi /etc/xinetd.d/cvs
</code>

<code>
# default: off
# description: The CVS service can record the history of your source \
#              files. CVS stores all the versions of a file in a single \
#              file in a clever way that only stores the differences \
#              between versions.
service cvspserver
{
	disable = no
	port                    = 2401
	socket_type             = stream
	protocol                = tcp
	wait                    = no
	user                    = root
	passenv                 = PATH
	server                  = /usr/bin/cvs
	env                     = HOME=/home/cvs/repo
	server_args             = -f --allow-root=/home/cvs/repo pserver
#       bind                    = 127.0.0.1
}
</code>

<code>
# vi /etc/xinetd.d/cvspserver
</code>

<code>
# default: on
# description: The cvs pserver
service cvspserver
{
	disable          = no
	flags            = REUSE
	socket_type      = stream
	wait             = no
	user             = cvs
	server           = /usr/bin/cvs
#   one more repository setting
#   server_args      = -f --allow-root=/env/cvs1 --allow-root=/env/cvs2 pserver
	server_args      = -f --allow-root=/home/cvs/repo pserver
	port             = 2401
	log_on_failure   += USERID
}
</code>

=== xinetd 재시작 ===
<code>
# service xinetd restart
</code>

=== 자동 시작 ===
<code>
# ntsysv
</code>
<code>
[*] xinetd 
</code>

=== CVSROOT 생성 ===
<code>
$ cvs -d /home/cvs/repo init
</code>

=== CVS 계정 ===
<code>
$ cd /home/cvs/repo/CVSROOT
$ vi cryptpasswd
</code>
<code>
#!/usr/bin/env perl
srand(time());
$randletter = "(int (rand (26)) + (int(rand(1) + .5)% 2?65:97))";
$salt = sprintf("%c%c",eval$randletter,eval$randletter);
$plaintext =shift;
$crypttext = crypt ($plaintext,$salt);
print "${crypttext}\n";
</code>
<code>
$ chmod 755 cryptpasswd
$ ./cryptpasswd PASSWORD
$ vi passwd
</code>
<code>
byforce:eoGKf0vOqTdvY:cvs
</code>

=== CVS 사용 ===
<code>
$ cd [프로젝트 디렉토리]
$ cvs -d :pserver:byforce@cvs.nalbam.com:/home/cvs/repo login
$ cvs -d :pserver:byforce@cvs.nalbam.com:/home/cvs/repo checkout java-src
</code>
